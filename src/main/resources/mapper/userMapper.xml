<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhc.community.dao.UserMapper">
    <sql id="insertFields">
        username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>

    <sql id="selectFields">
        id, username, password, salt, email, type, status, activation_code, header_url, create_time
    </sql>
    
    <insert id="registerUser" parameterType="User" keyProperty="id">
        INSERT INTO `user` <include refid="insertFields"></include>
        VALUES (#{username},#{password})
    </insert>


    <insert id="selectById" parameterType="User" keyProperty="id">
        select <include refid="selectFields"></include>
        from user
        where id = #{id}
    </insert>

    <insert id="selectByName" keyProperty="id">
        select <include refid="selectFields"></include>
        where username = #{username}
    </insert>

    <insert id="selectByEmail" keyProperty="id">
        select <include refid="selectFields"></include>
        from user
        where email = #{email}
    </insert>

    <insert id="insertUser" parameterType="User" keyProperty="id">
        insert into user (<include refid="insertfields"></include>)
        values(#{username}, #{password}, #{salt}, #{email}, #{type}, #{status}, #{activationCode}, #{headerUrl}, #{createTime})
    </insert>

    <update id="updateStatus">
        update user set status = #{status} where id = #{id}
    </update>

    <update id="updateHeader">
        update user set heade_url = #{headerUrl} where id = #{id}
    </update>

    <update id="updatePassword">
        update user set password = #{password} where id = #{id}
    </update>

</mapper>
